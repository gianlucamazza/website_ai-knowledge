# CI-optimized dependencies (minimal testing stack)
# Only essential packages for CI testing

# Testing framework (core only)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
pytest-cov==4.1.0
pytest-timeout==2.2.0

# Database testing (essential)
pytest-postgresql==6.0.0
sqlalchemy-utils==0.41.1

# Web testing (minimal)
httpx==0.25.2
responses==0.24.1

# Coverage
coverage[toml]==7.3.2

# Code quality (essential only)  
black==23.11.0
isort==5.12.0

# Security (stable version, NOT 3.x)
safety==2.3.5
bandit==1.7.5